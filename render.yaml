services:
  - type: web
    name: mailflow
    runtime: python
    buildCommand: ./build.sh
    startCommand: gunicorn app:app
    healthCheckPath: /
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.11

